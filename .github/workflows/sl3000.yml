name: Build ImmortalWrt SL-3000 (eMMC 128G)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "选择要编译的 ImmortalWrt 分支"
        required: true
        default: "openwrt-24.10"
      device:
        description: "设备型号"
        required: true
        default: "sl3000"
      config_type:
        description: "配置类型 (minimal/full)"
        required: true
        default: "full"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: 1. 清理磁盘空间
        run: |
          df -h
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo rm -rf /tmp/*
          sudo docker system prune -af || true
          sudo docker rmi $(docker images -q) || true
          df -h

      - name: 2. 拉取 ImmortalWrt 源码
        uses: actions/checkout@v4
        with:
          repository: ykm99999/immortalwrt
          ref: ${{ github.event.inputs.branch }}
          fetch-depth: 0

      - name: 3. 安装依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential gcc g++ make libncurses5-dev \
            libncursesw5-dev zlib1g-dev gawk git gettext unzip file libssl-dev \
            wget python3 rsync ccache parted e2fsprogs gpg python3-pip xz-utils zip libelf-dev time \
            perl flex bison libpcap-dev
          python3 -m pip install --user packaging

      - name: 4. 安装 CMake (优先 3.30.5，兼容最新)
        run: |
          sudo apt-get purge -y cmake cmake-data || true
          wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc | \
            sudo gpg --dearmor -o /usr/share/keyrings/kitware-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] \
            https://apt.kitware.com/ubuntu/ jammy main" | \
            sudo tee /etc/apt/sources.list.d/kitware.list
          sudo apt-get update
          if sudo apt-get install -y cmake=3.30.5-0kitware1ubuntu22.04.1 \
                                     cmake-data=3.30.5-0kitware1ubuntu22.04.1; then
            echo "✅ 已成功安装 CMake 3.30.5"
          else
            echo "⚠️ 3.30.5 不可用，安装最新版本"
            sudo apt-get install -y cmake
          fi
          cmake --version

      - name: 5. 拉取 overlay
        uses: actions/checkout@v4
        with:
          repository: ykm888/immortalwrt-sl3000
          path: overlay

      - name: 6. 应用 overlay 并复制配置文件
        env:
          TERM: xterm
        run: |
          cp overlay/target/linux/mediatek/dts/mt7981-siluo-sl3000.dts target/linux/mediatek/dts/
          cp overlay/target/linux/mediatek/image/mt7981.mk target/linux/mediatek/image/
          cp -r overlay/files ./files
          if [ "${{ github.event.inputs.config_type }}" = "minimal" ]; then
            # 复制你预设的 minimal 配置
            cp overlay/sl3000.minimal.config .config
            echo "✅ 使用 sl3000.minimal.config"
          else
            # 复制你预设的 full 配置
            cp overlay/sl3000.config .config
            echo "✅ 使用 sl3000.config"
          fi
          head -n 40 .config

      - name: 7. 校验 overlay 文件是否存在
        run: |
          test -f files/etc/fstab || { echo "❌ fstab 未复制"; exit 1; }
          test -f files/etc/uci-defaults/99-docker-mount || { echo "❌ docker-mount 脚本未复制"; exit 1; }
          echo "✅ overlay 文件已复制到固件目录"

      - name: 8. 更新 feeds
        run: |
          # 移除 scripts/feeds clean，直接更新和安装
          ./scripts/feeds update -a
          ./scripts/feeds install -a -f

      - name: 9. 下载源码
        run: |
          # 移除 rm -rf dl/* 以利用缓存
          make download -j$(nproc) || make download -j1 V=s
          df -h

      # --- 关键修改：将 make defconfig 替换为 make menuconfig ---
      - name: 10. Menuconfig 并校验平台
        env:
          TERM: xterm
        run: |
          # 使用 make menuconfig 来解析并自动填充配置依赖，但不会覆盖你的平台选择。
          make menuconfig
          
          # 校验平台配置是否依然存在 (通过了 menuconfig 这一步，这里应该能通过)
          grep -q 'CONFIG_TARGET_mediatek_mt7981=y' .config || { echo "❌ 未选择 mt7981 子平台"; exit 1; }
          grep -q 'CONFIG_TARGET_mediatek_mt7981_DEVICE.*sl3000=y' .config || { echo "❌ 未选择 SL-3000 设备"; exit 1; }
          echo "✅ 平台和设备选择校验通过！"
      # ------------------------------------------------------------------

      # --- 移除原有的 11. 清理 host cmake 步骤 (它是不必要的) ---

      - name: 11. 编译固件 (原 12)
        run: |
          df -h
          # 确保使用 /usr/bin/cmake 路径
          CMAKE=/usr/bin/cmake HOST_CMAKE=/usr/bin/cmake make -j$(nproc) IGNORE_ERRORS=m || \
          CMAKE=/usr/bin/cmake HOST_CMAKE=/usr/bin/cmake make -j1 V=s IGNORE_ERRORS=m
          df -h

      - name: 12. 收集固件 (原 13)
        run: |
          mkdir -p bin/firmware
          find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.tar.gz" \) -exec cp -v {} bin/firmware/ \;
          ls -lh bin/firmware
          sha256sum bin/firmware/* || true

      - name: 13. 上传固件 (原 14)
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-${{ github.event.inputs.branch }}-${{ github.event.inputs.device }}-emmc-firmware
          path: bin/firmware/*
          retention-days: 7

      - name: 14. 生成标签 (原 15)
        id: tag_calc
        run: |
          DATE_TAG=$(date +%Y%m%d)
          SHORT_HASH=$(git rev-parse --short HEAD)
          FULL_TAG="${{ github.event.inputs.device }}-${{ github.event.inputs.branch }}-${SHORT_HASH}-${DATE_TAG}"
          echo "tag=${FULL_TAG}" >> "$GITHUB_OUTPUT"
          echo "release_name=ImmortalWrt 固件 - ${{ github.event.inputs.device }} (eMMC, ${{ github.event.inputs.branch }})" >> "$GITHUB_OUTPUT"

      - name: 15. 发布固件 (原 16)
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag_calc.outputs.tag }}
          name: ${{ steps.tag_calc.outputs.release_name }}
          files: bin/firmware/*
          draft: false
          prerelease: false

      - name: 16. 清理旧 release (原 17)
        uses: dev-drprasad/delete-older-releases@v0.3.3
