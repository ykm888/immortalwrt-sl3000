name: build-sl3000-final-selfheal

on:
  workflow_dispatch:

env:
  WORKDIR: ${{ github.workspace }}/immortalwrt
  SOURCE_REPO: https://github.com/immortalwrt/immortalwrt.git
  SOURCE_BRANCH: openwrt-24.10

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 1. 深度优化磁盘空间
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /var/lib/docker
          sudo apt-get clean
          df -h

      - name: 2. 补齐系统依赖 (修复 Python 构建警告)
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential flex bison m4 gawk rsync unzip wget git-core \
            gettext libncurses-dev zlib1g-dev libssl-dev libelf-dev python3 python3-pip \
            python3-setuptools device-tree-compiler bc dos2unix

      - name: 3. 下载源码与自愈配置
        run: |
          git clone --depth=1 -b ${{ env.SOURCE_BRANCH }} ${{ env.SOURCE_REPO }} ${{ env.WORKDIR }}
          git clone https://github.com/${{ github.repository }}.git ${{ github.workspace }}/custom-config

      - name: 4. 执行初始化脚本
        working-directory: ${{ env.WORKDIR }}
        run: |
          mkdir -p staging_dir/host/stamp
          SCRIPT_FILE=$(find ${{ github.workspace }}/custom-config -name "clean-feeds.sh" | head -n 1)
          # 物理拷贝脚本，防止工作目录引用混乱
          cp -v "$SCRIPT_FILE" ./fix-env.sh
          chmod +x fix-env.sh
          ./fix-env.sh

      - name: 5. 编译基础工具链
        working-directory: ${{ env.WORKDIR }}
        run: |
          export PATH=${{ env.WORKDIR }}/staging_dir/host/bin:$PATH
          make tools/install -j$(nproc)
          make toolchain/install -j$(nproc)

      - name: 6. 编译场路径自愈 (解决 cc1/Image 报错)
        working-directory: ${{ env.WORKDIR }}
        run: |
          export PATH=${{ env.WORKDIR }}/staging_dir/host/bin:$PATH
          # 1. 展开内核，触发 files 覆盖机制
          make target/linux/prepare -j$(nproc)
          
          # 2. 定位缝合后的 DTS，地毯式同步 build_dir 路径
          SPLICED_DTS=$(find target/linux/mediatek -name "mt7981b-sl3000-emmc.dts" | head -n 1)
          for dts_dir in $(find build_dir -path "*/arch/arm64/boot/dts/mediatek" -type d); do
            echo "📥 暴力注入自愈 DTS 到: $dts_dir"
            cp -fv "$SPLICED_DTS" "$dts_dir/"
            # 自动补全内核 Makefile 生成规则
            sed -i '/dtb-$(CONFIG_ARCH_MEDIATEK)/a dtb-$(CONFIG_ARCH_MEDIATEK) += mt7981b-sl3000-emmc.dtb' "$(dirname "$dts_dir")/Makefile" || true
          done
          
          # 3. 预创 root-mediatek 目录
          mkdir -p build_dir/target-aarch64_cortex-a53_musl/root-mediatek
          echo "🚀 路径自愈完成。"

      - name: 7. 生产固件
        working-directory: ${{ env.WORKDIR }}
        run: |
          export PATH=${{ env.WORKDIR }}/staging_dir/host/bin:$PATH
          make defconfig
          # 优先安装内核及 DTB，失败则切换单线程打印详细报错
          make target/linux/install -j$(nproc) || make target/linux/install -j1 V=s
          make target/install -j$(nproc)

      - name: 8. 上传固件
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: SL3000-Fixed-Firmware
          path: ${{ env.WORKDIR }}/bin/targets/mediatek/filogic/*.bin
