name: build-sl3000-openwrt-2410

on:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build-sl3000-2410:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    env:
      DEVICE_ID: sl3000-emmc
      TARGET_PROFILE: mediatek_filogic
      DTS_NAME: mt7981b-sl-3000-emmc

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          path: repo

      - name: Prepare /mnt/openwrt
        run: |
          sudo mkdir -p /mnt/openwrt
          sudo chown -R $USER:$USER /mnt

      - name: Clone ImmortalWrt 24.10
        run: git clone --depth=1 -b openwrt-24.10 https://github.com/immortalwrt/immortalwrt.git /mnt/openwrt

      - name: Install build dependencies + ccache
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            python3 python3-pip python3-setuptools python3-wheel python3-dev python3-venv \
            libffi-dev libssl-dev ccache
          echo "export CCACHE_DIR=/mnt/openwrt/.ccache" >> $GITHUB_ENV
          echo "export CCACHE_COMPRESS=1" >> $GITHUB_ENV
          echo "export CCACHE_MAXSIZE=2G" >> $GITHUB_ENV

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: /mnt/openwrt/.ccache
          key: ${{ runner.os }}-openwrt-ccache-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-openwrt-ccache-

      - name: Cache dl
        uses: actions/cache@v4
        with:
          path: /mnt/openwrt/dl
          key: ${{ runner.os }}-openwrt-24.10-dl-${{ hashFiles('repo/sl3000/config/sl3000.config') }}
          restore-keys: |
            ${{ runner.os }}-openwrt-24.10-dl-

      - name: Cache toolchain & host
        uses: actions/cache@v4
        with:
          path: |
            /mnt/openwrt/staging_dir/toolchain
            /mnt/openwrt/staging_dir/host
          key: ${{ runner.os }}-openwrt-24.10-toolchain-${{ hashFiles('repo/sl3000/config/sl3000.config') }}
          restore-keys: |
            ${{ runner.os }}-openwrt-24.10-toolchain-

      - name: Clean feeds config
        working-directory: /mnt/openwrt
        run: |
          echo 'src-git packages https://github.com/openwrt/packages.git' > feeds.conf.default
          echo 'src-git luci https://github.com/openwrt/luci.git' >> feeds.conf.default
          echo 'src-git helloworld https://github.com/fw876/helloworld.git' >> feeds.conf.default
          echo 'src-git small https://github.com/kenzok8/small.git' >> feeds.conf.default

      - name: Update feeds
        working-directory: /mnt/openwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Run clean-feeds.sh (白名单模式)
        working-directory: /mnt/openwrt
        run: |
          chmod +x $GITHUB_WORKSPACE/repo/scripts/clean-feeds.sh
          bash $GITHUB_WORKSPACE/repo/scripts/clean-feeds.sh

      - name: Disable menuconfig globally
        run: |
          echo "KCONFIG_NONINTERACTIVE=1" >> $GITHUB_ENV
          echo "TERM=dumb" >> $GITHUB_ENV
          echo "LC_ALL=C" >> $GITHUB_ENV
          echo "FORCE_UNSAFE_CONFIGURE=1" >> $GITHUB_ENV
          echo "MAKEFLAGS=--no-builtin-rules" >> $GITHUB_ENV

      ######################################################################
      # ⭐ 企业级 Download sources（make download + 自愈）
      ######################################################################
      - name: Download sources (enterprise self-heal)
        working-directory: /mnt/openwrt
        run: |
          echo "=== Download: 第 1 次尝试（并行） ==="
          make download -j$(nproc) && exit 0

          echo "=== Download: 第 1 次失败 → 清理损坏文件 ==="
          find dl -type f -size 0 -delete || true
          find dl -type f -name "*.tmp" -delete || true

          echo "=== Download: 第 2 次尝试（并行） ==="
          make download -j$(nproc) && exit 0

          echo "=== Download: 第 2 次失败 → 清空 dl（最终自愈） ==="
          rm -rf dl/*

          echo "=== Download: 第 3 次尝试（最终） ==="
          make download -j$(nproc)

      ######################################################################

      - name: Build host tools (self-heal)
        working-directory: /mnt/openwrt
        run: |
          make tools/install -j$(nproc) V=s || \
          (make tools/clean && make tools/install -j$(nproc) V=s)

      - name: Build toolchain (self-heal)
        working-directory: /mnt/openwrt
        run: |
          make toolchain/install -j$(nproc) V=s || \
          (make toolchain/clean && make toolchain/install -j$(nproc) V=s)

      - name: Build kernel prepare (self-heal)
        working-directory: /mnt/openwrt
        run: |
          make target/linux/prepare -j$(nproc) V=s || \
          (make target/linux/clean && make target/linux/prepare -j$(nproc) V=s)

      - name: Build kernel (self-heal)
        working-directory: /mnt/openwrt
        run: |
          make target/linux/compile -j$(nproc) V=s || \
          (make target/linux/clean && make target/linux/compile -j$(nproc) V=s)

      - name: Build packages (self-heal)
        working-directory: /mnt/openwrt
        run: |
          make package/compile -j$(nproc) V=s || \
          (make package/clean && make package/compile -j$(nproc) V=s)

      - name: Detect three-piece consistency
        run: |
          echo "=== 三件套校验开始 ==="
          test -f repo/sl3000/dts/${DTS_NAME}.dts || (echo "❌ DTS missing" && exit 1)
          test -f repo/sl3000/mk/filogic-sl3000.mk || (echo "❌ MK missing" && exit 1)
          test -f repo/sl3000/config/sl3000.config || (echo "❌ CONFIG missing" && exit 1)
          grep -q "compatible" repo/sl3000/dts/${DTS_NAME}.dts || (echo "❌ DTS missing compatible" && exit 1)
          grep -q "mediatek" repo/sl3000/dts/${DTS_NAME}.dts || (echo "❌ DTS missing mediatek SoC" && exit 1)
          grep -q "model" repo/sl3000/dts/${DTS_NAME}.dts || echo "⚠ DTS missing model"
          grep -q "Device\/${DEVICE_ID}" repo/sl3000/mk/filogic-sl3000.mk || (echo "❌ MK device mismatch" && exit 1)
          grep -q "TARGET_DEVICES += ${DEVICE_ID}" repo/sl3000/mk/filogic-sl3000.mk || (echo "❌ MK target missing" && exit 1)
          grep -q "CONFIG_TARGET_${TARGET_PROFILE}_DEVICE_${DEVICE_ID}=y" repo/sl3000/config/sl3000.config || (echo "❌ CONFIG device missing" && exit 1)
          echo "✔ 三件套校验通过"

      - name: Auto fix three-piece naming
        run: |
          sed -i "s/Device\/sl3000-emmc/Device\/${DEVICE_ID}/g" repo/sl3000/mk/filogic-sl3000.mk || true
          sed -i "s/TARGET_DEVICES += sl3000-emmc/TARGET_DEVICES += ${DEVICE_ID}/g" repo/sl3000/mk/filogic-sl3000.mk || true
          sed -i "s/DEVICE_sl3000-emmc/DEVICE_${DEVICE_ID}/g" repo/sl3000/mk/filogic-sl3000.mk || true
          sed -i "s/CONFIG_TARGET_${TARGET_PROFILE}_DEVICE_sl3000-emmc/CONFIG_TARGET_${TARGET_PROFILE}_DEVICE_${DEVICE_ID}/g" repo/sl3000/config/sl3000.config || true
          if ! grep -q "model" repo/sl3000/dts/${DTS_NAME}.dts; then
            sed -i "1a model = \"SL3000 eMMC\";" repo/sl3000/dts/${DTS_NAME}.dts
          fi

      - name: Apply DTS
        run: |
          mkdir -p /mnt/openwrt/target/linux/mediatek/dts
          cp repo/sl3000/dts/${DTS_NAME}.dts /mnt/openwrt/target/linux/mediatek/dts/

      - name: Apply MK (auto-register)
        run: |
          MK=/mnt/openwrt/target/linux/mediatek/image/filogic.mk
          sed -i "/Device\/${DEVICE_ID}/,/endef/d" "$MK"
          sed -i "/TARGET_DEVICES += ${DEVICE_ID}/d" "$MK"
          cat repo/sl3000/mk/filogic-sl3000.mk >> "$MK"
          grep -q "Device\/${DEVICE_ID}" "$MK" || (echo '❌ 注册失败' && exit 1)
          grep -q "TARGET_DEVICES += ${DEVICE_ID}" "$MK" || (echo '❌ 注册失败' && exit 1)

      - name: Apply device config (.config)
        working-directory: /mnt/openwrt
        run: |
          rm -f .config
          cp $GITHUB_WORKSPACE/repo/sl3000/config/sl3000.config .config
          make defconfig

      - name: Build image (self-heal)
        working-directory: /mnt/openwrt
        run: |
          make target/image/compile -j$(nproc) V=s || \
          (make target/image/clean && make target/image/compile -j$(nproc) V=s)

      - name: Validate firmware output
        working-directory: /mnt/openwrt
        run: |
          ls -lh bin/targets/mediatek/filogic/*${DEVICE_ID}*sysupgrade.bin

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: sl3000-2410-firmware
          path: /mnt/openwrt/bin/targets/mediatek/filogic/*

      - name: Auto Release firmware
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: sl3000-openwrt-2410-${{ github.run_number }}
          name: SL3000 OpenWrt 24.10 Build #${{ github.run_number }}
          body: |
            OpenWrt 24.10 / Kernel 6.6
            Target: SL3000 (MT7981B eMMC)
            DEVICE_ID: sl3000-emmc
            Auto Register: Enhanced
            Auto Fix: Enhanced
            Auto Check: Enhanced
            Full Self-Heal: Enabled
            Build: ${{ github.run_number }}
            Commit: ${{ github.sha }}
          files: /mnt/openwrt/bin/targets/mediatek/filogic/*sysupgrade.bin

      - name: Cleanup workspace
        working-directory: /mnt/openwrt
        run: |
          rm -rf build_dir/* tmp/* || true
          find dl -type f -size +200M -delete || true
