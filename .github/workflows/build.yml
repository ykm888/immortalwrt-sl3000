name: build-sl3000-final-fixed

on:
  workflow_dispatch:

env:
  WORKDIR: ${{ github.workspace }}/immortalwrt
  SOURCE_REPO: https://github.com/immortalwrt/immortalwrt.git
  SOURCE_BRANCH: openwrt-24.10

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 1. 预装系统工具
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential flex bison m4 gawk libtool-bin rsync unzip wget curl patch git-core gettext libncurses-dev zlib1g-dev libelf-dev python3 device-tree-compiler bc dos2unix

      - name: 2. 获取源码
        run: |
          git clone --depth=1 -b ${{ env.SOURCE_BRANCH }} ${{ env.SOURCE_REPO }} ${{ env.WORKDIR }}
          git clone https://github.com/${{ github.repository }}.git ${{ github.workspace }}/custom-config

      - name: 3. 执行初始化脚本
        working-directory: ${{ env.WORKDIR }}
        run: |
          SCRIPT_FILE=$(find ${{ github.workspace }}/custom-config -name "clean-feeds.sh" | head -n 1)
          cp -v "$SCRIPT_FILE" ./clean-feeds.sh
          chmod +x clean-feeds.sh
          ./clean-feeds.sh

      - name: 4. 编译 Host 工具链
        working-directory: ${{ env.WORKDIR }}
        run: |
          export PATH=${{ env.WORKDIR }}/staging_dir/host/bin:$PATH
          make tools/install -j$(nproc)

      - name: 5. 编译交叉编译器
        working-directory: ${{ env.WORKDIR }}
        run: |
          export PATH=${{ env.WORKDIR }}/staging_dir/host/bin:$PATH
          make toolchain/install -j$(nproc)

      - name: 6. 内核 Prepare 与 编译现场修复
        working-directory: ${{ env.WORKDIR }}
        run: |
          export PATH=${{ env.WORKDIR }}/staging_dir/host/bin:$PATH
          # 1. 展开内核并同步 DTS files
          make target/linux/prepare -j$(nproc)
          
          # 2. 定位内核 DTS 编译目录
          REAL_K_DIR=$(find build_dir -path "*/arch/arm64/boot/dts/mediatek" -type d | head -n 1)
          SPLICED_DTS=$(find target/linux/mediatek -name "mt7981b-sl3000-emmc.dts" | head -n 1)
          
          # 3. 暴力注入 DTS 确保镜像阶段 cc1 能找到
          cp -fv "$SPLICED_DTS" "$REAL_K_DIR/"
          
          # 4. 修复内核 Makefile (解决 dtb 生成规则缺失)
          if ! grep -q "mt7981b-sl3000-emmc.dtb" "$REAL_K_DIR/Makefile"; then
              sed -i '/dtb-$(CONFIG_ARCH_MEDIATEK)/a dtb-$(CONFIG_ARCH_MEDIATEK) += mt7981b-sl3000-emmc.dtb' "$REAL_K_DIR/Makefile"
          fi
          
          # 5. 强制创建 root-mediatek 目录，防止 cp 报错
          mkdir -p build_dir/target-aarch64_cortex-a53_musl/root-mediatek
          echo "🚀 编译现场已锁定: $REAL_K_DIR"

      - name: 7. 最终固件编译
        working-directory: ${{ env.WORKDIR }}
        run: |
          export PATH=${{ env.WORKDIR }}/staging_dir/host/bin:$PATH
          make defconfig
          # 先单独执行一次内核安装，确保 dtb 优先生成
          make target/linux/install -j$(nproc) || make target/linux/install -j1 V=s
          make target/install -j$(nproc)

      - name: 8. 上传固件
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: SL3000-1GB-Fixed
          path: ${{ env.WORKDIR }}/bin/targets/mediatek/filogic/*.bin
